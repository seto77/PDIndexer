# 260406Cl PDIndexer 用に CSManager の release.yml を改修
# 260601Ch portable ZIP も同じリリースに添付するため、MSI を主配布のまま workflow 名を更新
# 260624Cl vdproj→WiX 移行 Phase B: 旧 vdproj ビルド経路を全廃し staging publish + dotnet build WiX に置換 (x64 のみ)
# 260625Cl WiX 移行 Phase C (計画書 §5 Phase C): 単一 job を 5 job (prepare/build-x64/build-arm64/smoke-arm64/release)
#   に再編し、arm64 (portable ZIP + MSI) を同一 run でビルド・windows-11-arm 実機で smoke して x64 と一緒に 1 回の
#   gh release create で原子的に公開する。ReciPro\.github\workflows\release.yml を移植。PDIndexer 固有の簡素化:
#     * Crystallography.Native を同梱しない (D-PDI-1) → devenv / native(vcxproj) ビルドが全 job で不要
#     * glfw3.dll は実行時未使用 (D-PDI-2) → arm64 publish に元々入らない。smoke の GL 起動回避ロジックも不要
#   ★不変条件「x64 は arm64 の成否に連動しない」を維持: release job は build-x64 成功のみ必須とし、arm64 (build/smoke) が
#   失敗したら x64 のみ公開して †脚注を残す (フォールバック)。FileVersion は prepare で 1 度算出し x64/arm64 双方へ渡して
#   完全一致させる (same-version cross-grade 要件、計画書 Phase A)。
name: Build And Release Installer And Portable ZIP

on:
  push:
    branches: [master]
    paths:
      - "PDIndexer/Version.cs"
  workflow_dispatch:
    inputs:
      force:
        description: "既存リリースを削除してリビルドする"
        type: boolean
        default: false
      dry_run:
        description: "x64+arm64 をビルド・smoke するが release は作らない (資産は artifact のみ)"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  # ---- 1. prepare: 版数解析・リリース要否判定・FileVersion 算出 (gh + git だけなので ubuntu) ----
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      needed: ${{ steps.version.outputs.needed }}
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      product_version: ${{ steps.version.outputs.product_version }}
      assembly_version: ${{ steps.version.outputs.assembly_version }}
      history_line: ${{ steps.version.outputs.history_line }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0 # 260625Cl assembly_version 用に GITHUB_SHA の commit 時刻を git log で引くため全履歴が要る

      - name: Parse version and check if release needed
        id: version
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          $content = Get-Content "PDIndexer/Version.cs" -Raw
          if ($content -notmatch 'ver(\d+\.\d+)\(') { throw "Could not parse version from Version.cs History." }
          $ver = $Matches[1]
          $tag = "v.$ver"
          $dryRun = $env:DRY_RUN -eq 'true'

          # 同じバージョンのリリースが既にあれば: dry_run は検証目的なので続行 (release job が create をスキップ)、
          #   force は既存を削除して続行、いずれでもなければスキップ。
          gh release view $tag 2>&1 | Out-Null
          $releaseExists = ($LASTEXITCODE -eq 0)
          $global:LASTEXITCODE = 0
          if ($releaseExists -and -not $dryRun) {
            if ("${{ inputs.force }}" -eq "true") {
              Write-Host "Force mode: deleting existing release $tag..."
              gh release delete $tag --yes
              git push origin :refs/tags/$tag 2>&1 | Out-Null
              $global:LASTEXITCODE = 0
            } else {
              Write-Host "Release $tag already exists. Skipping."
              "needed=false" >> $env:GITHUB_OUTPUT
              return
            }
          }

          # History の先頭行をリリースノートに使う (release job が notes を組む)
          $historyLine = ([regex]::Match($content, 'ver[^\r\n"]+').Value).Trim()

          # AssemblyVersion / FileVersion 用タイムスタンプ。x64⇔arm64 の same-version cross-grade には両 MSI の
          #   FileVersion 完全一致が必要 (計画書 Phase A 実測)。release commit (GITHUB_SHA) の commit 時刻にアンカーし
          #   決定論化する (同一 run の x64/arm64 がこの 1 値を共有。arm64 へは build-arm64-portable.yml の入力で渡す)。
          $now = ([System.DateTimeOffset]::Parse((git log -1 --format=%cI $env:GITHUB_SHA))).UtcDateTime
          $assemblyVer = "$($now.Year).$($now.Month).$($now.Day).$($now.ToString('HHmm'))"

          Write-Host "Version: $ver (tag: $tag, assembly: $assemblyVer, dryRun: $dryRun)"
          "needed=true"            >> $env:GITHUB_OUTPUT
          "version=$ver"           >> $env:GITHUB_OUTPUT
          "tag=$tag"               >> $env:GITHUB_OUTPUT
          "product_version=0.$ver" >> $env:GITHUB_OUTPUT
          "assembly_version=$assemblyVer" >> $env:GITHUB_OUTPUT
          "history_line=$historyLine" >> $env:GITHUB_OUTPUT

  # ---- 2. build-x64: x64 MSI (+旧名コピー) + portable ZIP ----
  build-x64:
    needs: prepare
    if: needs.prepare.outputs.needed == 'true'
    runs-on: windows-2025-vs2026
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive # 260625Cl shared libs are git submodules

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Restore managed dependencies
        shell: pwsh
        run: dotnet restore PDIndexer\PDIndexer.csproj

      # PDIndexer は Crystallography.Native を同梱しないため devenv / native ビルドは無い (D-PDI-1)。
      - name: Stage MSI payload (framework-dependent publish)
        shell: pwsh
        run: |
          $s = "artifacts\staging-msi-x64"
          dotnet publish PDIndexer\PDIndexer.csproj -c Release -p:Platform=x64 --self-contained false -o $s --no-restore `
            -p:AssemblyVersion=${{ needs.prepare.outputs.assembly_version }} -p:FileVersion=${{ needs.prepare.outputs.assembly_version }}
          if ($LASTEXITCODE -ne 0) { throw "x64 publish failed." }
          # glfw3.dll を除外 (D-PDI-2)
          Get-ChildItem $s -Filter "glfw3*.dll" | Remove-Item -Force
          Copy-Item PDIndexerSetup.Wix\LICENSE.rtf, PDIndexerSetup.Wix\REQUIREMENT.rtf $s
          Get-ChildItem $s -Filter *.pdb | Remove-Item
          if (Test-Path "$s\README-PORTABLE.txt") { throw "README-PORTABLE.txt leaked (portable marker must not be in MSI)" }
          if (Test-Path "$s\coreclr.dll") { throw "self-contained runtime leaked into framework-dependent staging" }
          if (Test-Path "$s\glfw3.dll") { throw "glfw3.dll leaked (must be excluded per D-PDI-2)" }
          if (-not (Test-Path "$s\PDIndexer.exe")) { throw "PDIndexer.exe missing from staging" }
          if (-not (Test-Path "$s\libxrl-11.dll")) { throw "libxrl-11.dll (xraylib native) missing from staging" }
          $expectedCultures = @('ja','de','fr','es','pt','it','ru','zh-Hans','zh-Hant','ko')
          $missingSat = $expectedCultures | Where-Object { -not (Get-ChildItem "$s\$_" -Filter *.resources.dll -ErrorAction SilentlyContinue) }
          if ($missingSat) { throw "satellite assemblies missing for culture(s): $($missingSat -join ', ')" }
          Write-Host "MSI staging OK: $((Get-ChildItem $s -Recurse -File).Count) files"

      - name: Build installer with WiX
        id: build_installer
        timeout-minutes: 10
        shell: pwsh
        run: |
          dotnet build PDIndexerSetup.Wix\PDIndexerSetup.wixproj -c Release -p:InstallerPlatform=x64 `
            -p:ProductVersion=${{ needs.prepare.outputs.product_version }} `
            -p:StagingDir="$PWD\artifacts\staging-msi-x64"
          if ($LASTEXITCODE -ne 0) { throw "WiX build failed (exit $LASTEXITCODE)." }
          # 主名称 PDIndexer-setup.msi + 旧名互換コピー PDIndexerSetup.msi (同一バイト、{software}Setup.msi 自動更新固定参照を維持)
          $msi = Get-Item "PDIndexerSetup.Wix\bin\Release\PDIndexer-setup.msi" -ErrorAction SilentlyContinue
          if (-not $msi) { throw "PDIndexer-setup.msi was not found." }
          Write-Host "MSI built: $($msi.FullName) ($($msi.Length) bytes)"
          "msi_path=$($msi.FullName)" >> $env:GITHUB_OUTPUT
          $legacy = Join-Path (Split-Path $msi.FullName) "PDIndexerSetup.msi"
          Copy-Item $msi.FullName $legacy -Force
          "legacy_msi_path=$legacy" >> $env:GITHUB_OUTPUT

      - name: Publish portable ZIP
        id: portable
        shell: pwsh
        run: |
          $publishDir = Join-Path $env:RUNNER_TEMP "PDIndexer-portable\PDIndexer"
          $zipPath = Join-Path (Get-Location) "PDIndexer-v.${{ needs.prepare.outputs.version }}.zip"
          if (Test-Path $publishDir) { Remove-Item -LiteralPath $publishDir -Recurse -Force }
          if (Test-Path $zipPath) { Remove-Item -LiteralPath $zipPath -Force }
          dotnet publish PDIndexer\PDIndexer.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:Platform=x64 `
            -p:AssemblyVersion=${{ needs.prepare.outputs.assembly_version }} `
            -p:FileVersion=${{ needs.prepare.outputs.assembly_version }} `
            -p:PublishSingleFile=false `
            -p:PublishTrimmed=false `
            -p:DebugType=None `
            -p:DebugSymbols=false `
            -o $publishDir
          if ($LASTEXITCODE -ne 0) { throw "x64 self-contained publish failed." }
          # glfw3.dll は portable ZIP からも除外 (D-PDI-2、MSI と同方針)
          Get-ChildItem $publishDir -Filter "glfw3*.dll" | Remove-Item -Force
          Copy-Item "LICENSE.md" $publishDir -Force
          Copy-Item "README-PORTABLE.txt" $publishDir -Force # portable 判定マーカー (MSI には入れない)
          Compress-Archive -Path (Join-Path (Split-Path $publishDir -Parent) "PDIndexer") -DestinationPath $zipPath -Force
          Write-Host "Portable ZIP built: $zipPath"
          "zip_path=$zipPath" >> $env:GITHUB_OUTPUT

      - name: Collect x64 release assets
        shell: pwsh
        run: |
          $out = Join-Path $env:RUNNER_TEMP "x64-out"
          New-Item -ItemType Directory -Force -Path $out | Out-Null
          Copy-Item "${{ steps.build_installer.outputs.msi_path }}" $out
          Copy-Item "${{ steps.build_installer.outputs.legacy_msi_path }}" $out
          Copy-Item "${{ steps.portable.outputs.zip_path }}" $out
          Get-ChildItem $out | ForEach-Object { Write-Host "x64 asset: $($_.Name) ($($_.Length) bytes)" }

      - name: Upload x64 release assets
        uses: actions/upload-artifact@v6
        with:
          name: x64-release-assets
          path: ${{ runner.temp }}/x64-out

  # ---- 3. build-arm64: reusable build (二重保守なし)。prepare の FileVersion を渡して x64 と一致させる ----
  build-arm64:
    needs: prepare
    if: needs.prepare.outputs.needed == 'true'
    uses: ./.github/workflows/build-arm64-portable.yml
    with:
      assembly_version: ${{ needs.prepare.outputs.assembly_version }}
    permissions:
      contents: read

  # ---- 4. smoke-arm64: windows-11-arm 実機で smoke → 検証済み ZIP+MSI を artifact 化 ----
  # このジョブが失敗しても release job は走る (フォールバックで x64 のみ公開)。想定故障は全て「ビルド緑・実行時死亡」型なので
  # PE 検査 + LoadLibrary + PDIndexer.exe --smoke (xraylib 明示アサート) + GUI 起動 + MSI install/smoke を実機で行う。
  smoke-arm64:
    needs: [prepare, build-arm64]
    if: needs.prepare.outputs.needed == 'true'
    runs-on: windows-11-arm # arm64 ネイティブランナー (public repo 無料)
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive # native\win-arm64\ の LoadLibrary 検査用 (バンドル内 DLL と同一ソース)

      - name: Resolve arm64 asset names
        id: names
        shell: pwsh
        run: |
          $ver = "${{ needs.prepare.outputs.version }}"
          "zip_name=PDIndexer-v.${ver}_arm64.zip" >> $env:GITHUB_OUTPUT
          "msi_name=PDIndexer-setup_arm64.msi"    >> $env:GITHUB_OUTPUT

      - name: Verify version consistency between prepare and build
        shell: pwsh
        run: |
          if ('${{ needs.prepare.outputs.version }}' -ne '${{ needs.build-arm64.outputs.version }}') {
            throw "Version mismatch: prepare=${{ needs.prepare.outputs.version }} build=${{ needs.build-arm64.outputs.version }}"
          }

      - name: Download portable artifact
        uses: actions/download-artifact@v7
        with:
          name: ${{ needs.build-arm64.outputs.artifact_name }}
          path: ${{ runner.temp }}/arm64-artifact

      - name: Download arm64 MSI artifact
        uses: actions/download-artifact@v7
        with:
          name: ${{ needs.build-arm64.outputs.msi_artifact_name }}
          path: ${{ runner.temp }}/arm64-msi

      - name: "Smoke 1: PE machine + native LoadLibrary (arm64-native pwsh)"
        shell: pwsh
        run: |
          $dir = Join-Path $env:RUNNER_TEMP "arm64-artifact/PDIndexer"
          if (-not (Test-Path (Join-Path $dir "PDIndexer.exe"))) { throw "Artifact layout unexpected: PDIndexer.exe not found in $dir" }
          function Get-PEMachine([string]$path) {
            $b = [IO.File]::ReadAllBytes($path)
            return [BitConverter]::ToUInt16($b, [BitConverter]::ToInt32($b, 0x3C) + 4)
          }
          # PDIndexer は Crystallography.Native を持たないため検査対象は exe と libxrl のみ。glfw は不在が正常 (D-PDI-2)。
          if (Test-Path (Join-Path $dir "glfw3.dll")) { throw "glfw3.dll present in arm64 portable (must be excluded, D-PDI-2)" }
          foreach ($f in @("PDIndexer.exe", "libxrl-11.dll")) {
            $m = Get-PEMachine (Join-Path $dir $f)
            "{0}: machine=0x{1:X4}" -f $f, $m
            if ($m -ne 0xAA64) { throw "$f is not ARM64 (machine=0x$($m.ToString('X4')))" }
          }
          # arm64 ネイティブの pwsh で実 LoadLibrary (= AA64 PE がクリーン環境で本当にロードできるか)。
          # 配布物内の実物は後続の --smoke (xraylib self-test) が検査するので、ここはリポ側 native\win-arm64\ を検査。
          $abs = (Resolve-Path "PDIndexer\native\win-arm64\libxrl-11.dll").Path
          $h = [System.Runtime.InteropServices.NativeLibrary]::Load($abs)
          [System.Runtime.InteropServices.NativeLibrary]::Free($h)
          Write-Host "LoadLibrary OK: $abs"

      - name: "Smoke 2: PDIndexer.exe --smoke (Xraylib explicit assertion)"
        shell: pwsh
        run: |
          $dir = Join-Path $env:RUNNER_TEMP "arm64-artifact/PDIndexer"
          $out = Join-Path $env:RUNNER_TEMP "smoke-result.txt"
          $p = Start-Process -FilePath (Join-Path $dir "PDIndexer.exe") -ArgumentList @("--smoke", "`"$out`"") -WorkingDirectory $dir -PassThru
          if (-not $p.WaitForExit(600000)) { Stop-Process -Id $p.Id -Force; throw "--smoke did not finish within 10 minutes." }
          if (-not (Test-Path $out)) { throw "--smoke produced no output file (exit $($p.ExitCode))." }
          $result = Get-Content $out -Raw
          Write-Host $result
          if ($p.ExitCode -ne 0) { throw "--smoke reported failure (exit $($p.ExitCode)). See output above." }
          foreach ($expect in @("arch=Arm64", "xraylibEnabled=True")) {
            if ($result -notmatch [regex]::Escape($expect)) { throw "--smoke output missing expected '$expect'." }
          }

      - name: "Smoke 3: GUI launch (main window appears and survives)"
        shell: pwsh
        run: |
          # 起動即死の検出。PDIndexer は OpenGL を使わない (GuiCapture.cs) ため ReciPro の GL 無効化回避ロジックは不要。
          # FormMain_Load は全子フォームを構築してから FormMain を表示するため、EnumWindows でプロセスの全トップレベル
          # ウィンドウを定期確認し「unowned + visible + タイトル 'PDIndexer*'」(= Load 完了後の FormMain) を合格条件にする。
          Add-Type -TypeDefinition @'
          using System;
          using System.Collections.Generic;
          using System.Runtime.InteropServices;
          using System.Text;
          public static class WinEnum {
            delegate bool EnumProc(IntPtr h, IntPtr lp);
            [DllImport("user32.dll")] static extern bool EnumWindows(EnumProc cb, IntPtr lp);
            [DllImport("user32.dll")] static extern uint GetWindowThreadProcessId(IntPtr h, out uint pid);
            [DllImport("user32.dll")] static extern bool IsWindowVisible(IntPtr h);
            [DllImport("user32.dll", CharSet = CharSet.Unicode)] static extern int GetWindowText(IntPtr h, StringBuilder sb, int n);
            [DllImport("user32.dll")] static extern IntPtr GetWindow(IntPtr h, uint cmd);
            public static List<string> List(int pid) {
              var r = new List<string>();
              EnumWindows((h, lp) => {
                uint wpid; GetWindowThreadProcessId(h, out wpid);
                if (wpid == pid) {
                  var sb = new StringBuilder(512); GetWindowText(h, sb, 512);
                  r.Add((IsWindowVisible(h) ? "visible" : "hidden") + "|" + (GetWindow(h, 4) != IntPtr.Zero ? "owned" : "unowned") + "|" + sb);
                }
                return true;
              }, IntPtr.Zero);
              return r;
            }
          }
          '@
          $dir = Join-Path $env:RUNNER_TEMP "arm64-artifact/PDIndexer"
          $p = Start-Process -FilePath (Join-Path $dir "PDIndexer.exe") -WorkingDirectory $dir -PassThru
          $deadline = (Get-Date).AddSeconds(480) # 初回起動は全子フォーム構築 + JIT で数分かかり得る
          $shown = $false
          $lastLog = [DateTime]::MinValue
          while ((Get-Date) -lt $deadline) {
            if ($p.HasExited) { throw "PDIndexer.exe exited prematurely (exit $($p.ExitCode))." }
            $windows = [WinEnum]::List($p.Id)
            if ($windows | Where-Object { $_ -like 'visible|unowned|PDIndexer*' }) { $shown = $true; break }
            if (((Get-Date) - $lastLog).TotalSeconds -ge 30) {
              $p.Refresh()
              Write-Host ("t={0:f0}s cpu={1:f1}s windows: {2}" -f ((Get-Date) - $p.StartTime).TotalSeconds, $p.TotalProcessorTime.TotalSeconds, ($windows -join ' / '))
              $lastLog = Get-Date
            }
            Start-Sleep -Seconds 3
          }
          if (-not $shown) {
            Write-Host "Final window list: $(([WinEnum]::List($p.Id)) -join ' / ')"
            Add-Type -AssemblyName System.Windows.Forms, System.Drawing
            $bounds = [System.Windows.Forms.SystemInformation]::VirtualScreen
            $bmp = New-Object System.Drawing.Bitmap $bounds.Width, $bounds.Height
            [System.Drawing.Graphics]::FromImage($bmp).CopyFromScreen($bounds.X, $bounds.Y, 0, 0, $bmp.Size)
            $bmp.Save((Join-Path $env:RUNNER_TEMP "smoke3-desktop.png"))
            Stop-Process -Id $p.Id -Force
            throw "PDIndexer main window did not appear within 480 s (see logged window lists and smoke3-desktop.png artifact)."
          }
          Write-Host "Main window appeared: $(([WinEnum]::List($p.Id) | Where-Object { $_ -like 'visible|*' }) -join ' / ')"
          Start-Sleep -Seconds 10 # 表示直後のクラッシュ検出
          if ($p.HasExited) { throw "PDIndexer.exe crashed shortly after showing the main window (exit $($p.ExitCode))." }
          Stop-Process -Id $p.Id -Force
          Write-Host "GUI launch smoke OK."

      - name: "Smoke 4: arm64 MSI (per-user install, --smoke, uninstall)"
        shell: pwsh
        run: |
          # MSI の Template/UpgradeCode/ProductVersion 検査はビルド側 (x64 ランナー、build-arm64-portable.yml) で実施済み:
          #   windows-11-arm の COM 遅延バインディングは OpenDatabase が DISP_E_TYPEMISMATCH で失敗する (ReciPro 実測)。
          #   ここでは COM を使わず install/uninstall とも MSI パス指定で行う。
          $msi = Join-Path $env:RUNNER_TEMP "arm64-msi/${{ steps.names.outputs.msi_name }}"
          if (-not (Test-Path $msi)) { throw "MSI artifact not found: $msi" }

          # MSI 版は framework-dependent → smoke 実行に .NET 10 Desktop Runtime (arm64) が必要。ランナーに無ければ導入。
          $hasDesktop = $false
          try { $hasDesktop = ((dotnet --list-runtimes) | Select-String "Microsoft.WindowsDesktop.App 10\.") -ne $null } catch {}
          if (-not $hasDesktop) {
            Write-Host "Installing .NET 10 Desktop Runtime (arm64) for the smoke test..."
            Invoke-WebRequest https://dot.net/v1/dotnet-install.ps1 -OutFile "$env:RUNNER_TEMP\dotnet-install.ps1"
            & "$env:RUNNER_TEMP\dotnet-install.ps1" -Runtime windowsdesktop -Channel 10.0 -Architecture arm64 -InstallDir "$env:RUNNER_TEMP\dotnet"
            $env:DOTNET_ROOT = "$env:RUNNER_TEMP\dotnet"
          }

          # per-user silent install (ALLUSERS/MSIINSTALLPERUSER は渡さない = per-user 固定パッケージの既定)
          $p = Start-Process msiexec -ArgumentList '/i', "`"$msi`"", '/qn', '/l*v', "$env:RUNNER_TEMP\msi-install.log" -Wait -PassThru
          if ($p.ExitCode -ne 0) { throw "MSI install failed (exit $($p.ExitCode)). See msi-install.log artifact." }
          $appDir = "$env:LOCALAPPDATA\Crystallography Software\PDIndexer"
          if (-not (Test-Path "$appDir\PDIndexer.exe")) { throw "installed PDIndexer.exe missing in $appDir" }

          # インストール実体で --smoke (黙殺フォールバック型故障の検出。Smoke 2 と同一アサート)
          $out = Join-Path $env:RUNNER_TEMP "msi-smoke-result.txt"
          $p = Start-Process -FilePath "$appDir\PDIndexer.exe" -ArgumentList @("--smoke", "`"$out`"") -WorkingDirectory $appDir -PassThru
          if (-not $p.WaitForExit(600000)) { Stop-Process -Id $p.Id -Force; throw "--smoke (MSI install) did not finish within 10 minutes." }
          if (-not (Test-Path $out)) { throw "--smoke produced no output file (exit $($p.ExitCode))." }
          $result = Get-Content $out -Raw
          Write-Host $result
          if ($p.ExitCode -ne 0) { throw "--smoke reported failure (exit $($p.ExitCode))." }
          foreach ($expect in @("arch=Arm64", "xraylibEnabled=True")) {
            if ($result -notmatch [regex]::Escape($expect)) { throw "--smoke output missing expected '$expect'." }
          }

          # アンインストールしてランナーを汚さない。ProductCode は不要 (MSI パス指定で /x できる)
          $p = Start-Process msiexec -ArgumentList '/x', "`"$msi`"", '/qn', '/l*v', "$env:RUNNER_TEMP\msi-uninstall.log" -Wait -PassThru
          if ($p.ExitCode -ne 0) { throw "MSI uninstall failed (exit $($p.ExitCode))." }
          if (Test-Path "$appDir\PDIndexer.exe") { throw "uninstall left PDIndexer.exe behind" }
          Write-Host "MSI smoke OK (install -> --smoke -> uninstall)."

      - name: Upload smoke diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: smoke-arm64-diagnostics
          path: |
            ${{ runner.temp }}/smoke3-desktop.png
            ${{ runner.temp }}/smoke-result.txt
            ${{ runner.temp }}/msi-smoke-result.txt
            ${{ runner.temp }}/msi-install.log
            ${{ runner.temp }}/msi-uninstall.log
          if-no-files-found: ignore

      - name: Create release ZIP and stage arm64 assets
        shell: pwsh
        run: |
          # x64 portable ZIP (build-x64) と同じ構造 (ZIP 直下に PDIndexer\ フォルダ) に合わせる
          $out = Join-Path $env:RUNNER_TEMP "arm64-out"
          New-Item -ItemType Directory -Force -Path $out | Out-Null
          $zip = Join-Path $out "${{ steps.names.outputs.zip_name }}"
          Compress-Archive -Path (Join-Path $env:RUNNER_TEMP "arm64-artifact/PDIndexer") -DestinationPath $zip -Force
          $msi = Join-Path $env:RUNNER_TEMP "arm64-msi/${{ steps.names.outputs.msi_name }}"
          if (-not (Test-Path $msi)) { throw "MSI not found: $msi" }
          Copy-Item $msi $out
          Get-ChildItem $out | ForEach-Object { Write-Host "arm64 asset: $($_.Name) ($($_.Length) bytes)" }

      - name: Upload arm64 release assets
        uses: actions/upload-artifact@v6
        with:
          name: arm64-release-assets
          path: ${{ runner.temp }}/arm64-out

  # ---- 5. release: x64 必須・arm64 は揃っていれば一緒に、1 回の gh release create で原子的に公開 ----
  release:
    needs: [prepare, build-x64, build-arm64, smoke-arm64]
    if: ${{ !cancelled() && needs.prepare.outputs.needed == 'true' && needs.build-x64.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Download x64 release assets
        uses: actions/download-artifact@v7
        with:
          name: x64-release-assets
          path: ${{ runner.temp }}/x64

      - name: Download arm64 release assets
        if: ${{ needs.build-arm64.result == 'success' && needs.smoke-arm64.result == 'success' }}
        uses: actions/download-artifact@v7
        with:
          name: arm64-release-assets
          path: ${{ runner.temp }}/arm64

      - name: Create tag and GitHub Release
        id: create
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRY_RUN: ${{ inputs.dry_run }}
          ARM64_BUILD: ${{ needs.build-arm64.result }}
          ARM64_SMOKE: ${{ needs.smoke-arm64.result }}
          HISTORY_LINE: ${{ needs.prepare.outputs.history_line }}
        run: |
          $ver = "${{ needs.prepare.outputs.version }}"
          $tag = "${{ needs.prepare.outputs.tag }}"
          $historyLine = $env:HISTORY_LINE
          $dryRun = $env:DRY_RUN -eq 'true'
          $arm64Ok = ($env:ARM64_BUILD -eq 'success') -and ($env:ARM64_SMOKE -eq 'success')

          $x64 = Join-Path $env:RUNNER_TEMP "x64"
          # 260625Cl 主名称 PDIndexer-setup.msi + 旧名コピー PDIndexerSetup.msi (旧クライアント自動更新互換) + portable ZIP
          $assets = @(
            (Join-Path $x64 "PDIndexer-setup.msi")
            (Join-Path $x64 "PDIndexerSetup.msi")
            (Join-Path $x64 "PDIndexer-v.$ver.zip")
          )

          # arm64 が揃っていれば 4 資産を atomic 公開し †脚注なし。揃わなければ x64 のみ公開し arm64 行は残して †脚注で説明。
          if ($arm64Ok) {
            $arm64 = Join-Path $env:RUNNER_TEMP "arm64"
            $assets += (Join-Path $arm64 "PDIndexer-v.${ver}_arm64.zip")
            $assets += (Join-Path $arm64 "PDIndexer-setup_arm64.msi")
            $mark = ""
            $footnote = @()
          } else {
            Write-Warning "ARM64 build/smoke did not succeed (build=$env:ARM64_BUILD smoke=$env:ARM64_SMOKE); publishing x64-only with footnote."
            $mark = "&dagger;"
            $footnote = @("", "&dagger; Arm64 files are attached after on-device verification and may appear slightly later than the x64 files.")
          }

          $notes = @(
            $historyLine
            ""
            "| Download | Description |"
            "|---|---|"
            "| **[PDIndexer-setup.msi](https://github.com/seto77/PDIndexer/releases/download/v.$ver/PDIndexer-setup.msi)** | **Recommended.** Installer for ordinary (x64) Windows PCs. |"
            "| [PDIndexer-setup_arm64.msi](https://github.com/seto77/PDIndexer/releases/download/v.$ver/PDIndexer-setup_arm64.msi) | Installer for Windows on Arm (Snapdragon PCs, or Apple Silicon Macs running Windows via virtualization, etc.).$mark |"
            "| [PDIndexer-v.$ver.zip](https://github.com/seto77/PDIndexer/releases/download/v.$ver/PDIndexer-v.$ver.zip) | Portable (x64): no installation, self-contained. Suitable for PCs where you have no administrator rights. |"
            "| [PDIndexer-v.${ver}_arm64.zip](https://github.com/seto77/PDIndexer/releases/download/v.$ver/PDIndexer-v.${ver}_arm64.zip) | Portable for Windows on Arm: no installation, no administrator rights needed.$mark |"
            "| PDIndexerSetup.msi | Identical to PDIndexer-setup.msi (legacy name for auto-update from older versions). |"
          ) + $footnote
          $notesPath = Join-Path $env:RUNNER_TEMP "release-notes.txt"
          $notes | Set-Content -Path $notesPath -Encoding UTF8

          foreach ($a in $assets) { if (-not (Test-Path $a)) { throw "Release asset missing: $a" } }

          if ($dryRun) {
            Write-Host "=== DRY RUN: would 'gh release create $tag' with assets: ==="
            $assets | ForEach-Object { Write-Host "  $_" }
            Write-Host "=== notes ==="; Get-Content $notesPath | ForEach-Object { Write-Host $_ }
            $stage = Join-Path $env:RUNNER_TEMP "release-staged"
            New-Item -ItemType Directory -Force -Path $stage | Out-Null
            $assets | ForEach-Object { Copy-Item $_ $stage }
            Copy-Item $notesPath $stage
            "staged=$stage" >> $env:GITHUB_OUTPUT
            return
          }

          # 原子的: tag 未存在時に --target のコミットへ lightweight tag を作成し release と一括で作る (orphan tag を防ぐ)
          gh release create $tag $assets `
            --target $env:GITHUB_SHA `
            --title "PDIndexer $tag" `
            --notes-file $notesPath
          if ($LASTEXITCODE -ne 0) { throw "gh release create failed (exit $LASTEXITCODE)." }
          Write-Host "Released $tag with $($assets.Count) assets (arm64Ok=$arm64Ok)."

      - name: Upload staged release assets (dry-run inspection)
        if: ${{ inputs.dry_run }}
        uses: actions/upload-artifact@v6
        with:
          name: release-staged-${{ needs.prepare.outputs.tag }}
          path: ${{ steps.create.outputs.staged }}
